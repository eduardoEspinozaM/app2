<%= form_for(@categoria) do |f| %>
  <% if @categoria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categoria.errors.count, "error") %> prohibited this categoria from being saved:</h2>

      <ul>
      <% @categoria.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="col-lg-6">
<div class="input-group ">
  <span class="control-label col-md-3 col-sm-3 col-xs-12" ><%= f.label :carrera %></span>
  
  <%= f.text_field :carrera, class:"form-control" %>
 
</div>

 <div class="actions ">
    <%= f.submit class:"btn btn-round btn-success fa fa-trash-o fa-lg" %>
  </div>
</div>

<% end %>
